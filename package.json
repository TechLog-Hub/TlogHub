{
  "name": "techlog-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Techlog Hub monorepo root tooling.",
  "scripts": {
    "prepare": "husky || true",
    "validate:repo": "node scripts/validate-repo.mjs",
    "validate:commit-message": "node scripts/validate-commit-message.mjs"
  },
  "devDependencies": {
    "husky": "9.1.7"
  }
}
